# AZM-QOS v2.1 Term Audit Guide

The term audit CSV contains:

```text
quantity, i, j, label, pauli, coeff_real, coeff_imag, n_qubits
```

Use it to check that all M/V entries are present, Pauli strings have the correct qubit count, and coefficients match your derivation.
